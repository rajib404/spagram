services:
  app:
    build: .
    container_name: spagram-app
    restart: unless-stopped
    ports:
      - "3001:3000"
    env_file:
      - .env.production
    extra_hosts:
      - "host.docker.internal:host-gateway"
